<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>12</LangVersion>
    <Nullable>enable</Nullable>
    <Version>1.0.0</Version>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>

    <!-- Installs to BepInEx\plugins\RepoCheque\ (no author name in the path). -->
    <PluginOutputSubdirectory>RepoCheque</PluginOutputSubdirectory>
  </PropertyGroup>

  <!-- Add Package Sources -->
  <PropertyGroup>
    <RestoreAdditionalProjectSources>
      https://api.nuget.org/v3/index.json;
      https://nuget.bepinex.dev/v3/index.json;
      https://nuget.windows10ce.com/nuget/v3/index.json
    </RestoreAdditionalProjectSources>
  </PropertyGroup>

  <!-- Keep local-only files out of the shipped plugin folder -->
  <ItemGroup>
    <None Remove="Directory.Repo.props" />
    <None Remove="artwork\**" />
    <!-- Ship the cheque artwork next to the DLL so the mod picks it up.
         Update (not Include) because the SDK already globs these files in as None. -->
    <None Update="*.png;*.zip" CopyToPluginOutputDirectory="Never" />
    <None Update="*Front*.png;*Back*.png;cheque*.png;icon.png" CopyToPluginOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- Package References -->
  <ItemGroup>
    <PackageReference Include="BepInEx.Analyzers" Version="1.*" PrivateAssets="all"/>
    <PackageReference Include="BepInEx.AssemblyPublicizer.MSBuild" Version="0.4.1" PrivateAssets="all" IncludeAssets="build" />
    <PackageReference Include="Linkoid.Repo.Plugin.Build" Version="*" PrivateAssets="all" />
    
    <PackageReference Include="BepInEx.Core" Version="5.*" ExcludeAssets="runtime" />
    <PackageReference Include="UnityEngine.Modules" Version="2022.3.62" IncludeAssets="compile" PrivateAssets="all"/>
    <PackageReference Include="R.E.P.O.GameLibs.Steam" Version="*-*" PrivateAssets="all" Publicize="true" />
  </ItemGroup>

</Project>
